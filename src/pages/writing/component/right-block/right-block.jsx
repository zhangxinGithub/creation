import React, { useState, useEffect, useImperativeHandle } from 'react';
import { ajax } from '@/api/ajax';

const Editor = (props, ref) => {
  return <div className="editor">右侧菜单</div>;
};

export default Editor;
